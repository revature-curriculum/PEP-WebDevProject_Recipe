/**
 * This script defines the CRUD operations for Recipe objects in the Recipe Management Application.
 */

/**
 * 1. Get references to various DOM elements, such as buttons and input elements, using the document object's `getElementById()` method.
*/

/**
 * 2. Ensure the buttons have their 'onclick' events associated with a corresponding function.
*/

/**
 * 3. Create an object, like an array, to keep track of recipes.
*/

/**
 * 4. Ensure that, on start up, the recipe list is refreshed.
refreshRecipeList();
*/

/**
 * 5. Provide implementations to the below four functions.
*/

/**
 * Function to add a new recipe to the list.
 * 
 * This function collects input from the user for a new recipe's name and ingredients, creates a recipe object, adds it to the recipes array, and then refreshes the displayed recipe list.
 */
function addRecipe(){

}

/**
 * Function to update an existing recipe's ingredients.
 * 
 * This function searches for a recipe by its name within the recipes array. If a recipe with the matching name is found, it updates the ingredients of that recipe. Finally, it refreshes the displayed recipe list to reflect changes.
 */
function updateRecipe(){
    
}

/**
 * Function to delete a recipe from the list.
 * 
 * This function removes a recipe from the recipes array based on the recipe name input by the user and then refreshes the displayed recipe list.
 */
function deleteRecipe(){
    
}

/**
 * Function to refresh and display the list of recipes on the webpage.
 * 
 * This function clears the existing recipe display in the UI and rebuilds it from the current state of the recipes array, displaying each recipe's name and ingredients.
 */
function refreshRecipeList(){
    
}

